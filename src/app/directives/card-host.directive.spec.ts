import { CardHostDirective } from './card-host.directive';

describe('CardHostDirective', () => {
  it('should create an instance', () => {
    const directive = new CardHostDirective();
    expect(directive).toBeTruthy();
  });
});
